name: catdog-env
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  
  - pip:
      - tensorflow==2.15
      - matplotlib
      - numpy
      - pandas
      - scikit-learn
      - seaborn
