# 重新生成 environment.yml 內容
environment_yml_content = '''name: catdog-env
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - tensorflow
      - matplotlib
      - numpy
'''

# 儲存檔案
file_path = "/mnt/data/environment.yml"
with open(file_path, "w") as f:
    f.write(environment_yml_content)

file_path
