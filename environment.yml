# 重新生成乾淨正確的 environment.yml 內容
correct_environment_yml = """name: catdog-env
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - tensorflow
      - matplotlib
      - numpy
"""

file_path = "/mnt/data/environment.yml"
with open(file_path, "w") as f:
    f.write(correct_environment_yml)

file_path
